%toc

= 度量时间差 =
内核通过定时器中断来跟踪时间流。时钟中断由系统定时硬件以周期性的间隔产生， 这个间隔由内核根据HZ的值设定， HZ是一个与体系结构有关的常数。
如果想改变系统时钟中断发生的频率， 可以通过修改HZ值实现， 但要重新编译内核以及所有模块。
每次当时钟中断发生时， 内核内部计数器的值就会增加一。这个计数器值在系统引导时初始化为0， 它的值就是自上次操作系统引导以来的时钟滴答数(jiffies)

HZ表示1秒中产生多少次中断， jiffy表示从系统启动开始的中断次数
jiffies = n * HZ,   n为秒

= 使用jiffies计数器 =

